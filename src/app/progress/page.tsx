import { Shell } from '@/components/layout/Shell';

export default function ProgressPage() {
  return (
    <Shell title="Progress">
      <div style={{ paddingTop: '20px' }}>
        <p style={{ color: 'var(--fg-muted)', fontSize: '15px' }}>
          Progress dashboard — coming in Phase 5
        </p>
      </div>
    </Shell>
  );
}
