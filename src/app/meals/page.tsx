import { Shell } from '@/components/layout/Shell';

export default function MealsPage() {
  return (
    <Shell title="Meals & Soak">
      <div style={{ paddingTop: '20px' }}>
        <p style={{ color: 'var(--fg-muted)', fontSize: '15px' }}>
          Meals & Soak display — coming in Phase 6
        </p>
      </div>
    </Shell>
  );
}
